class ProfilesController < ApplicationController
  def show
    # @pics = User.find_by(user_name:params[:user_name]).pics.order
                                                  # ('created_at DESC')
  end
end
